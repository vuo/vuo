Creates a Window Property that changes the size of a window.

   - `Width` and `Height` — The size of the window's content area.
   - `Unit` — Whether the width and height are specified in points or pixels.
