Only lets a MIDI pitch bend pass through if its channel matches.

   - `Pitch Bend` — An event into this port is blocked unless the port's value matches the specified channel.
   - `Channel` — Which channel to accept.
