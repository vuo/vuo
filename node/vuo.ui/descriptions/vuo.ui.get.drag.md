Outputs information about a drag event from the `Receive File Drags` node.

   - `Position` — The drag event's location, in Vuo Coordinates.
   - `URLs` — A list of the file URLs being dragged into the window.
