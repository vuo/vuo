Finds all hands in the list that have a certain ID.

   - `hands` — The list of hands to search in.
   - `id` — The ID to search for. 
   - `filteredHands` — All items from `hands` that have ID `id`.
